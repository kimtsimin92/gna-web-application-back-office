import { LoginFirstTimeForm } from './login-first-time-form';

describe('LoginFirstTimeForm', () => {
  it('should create an instance', () => {
    expect(new LoginFirstTimeForm()).toBeTruthy();
  });
});
