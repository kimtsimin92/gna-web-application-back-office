import { ReinsuranceResponseDto } from './reinsurance-response-dto';

describe('ReinsuranceResponseDto', () => {
  it('should create an instance', () => {
    expect(new ReinsuranceResponseDto()).toBeTruthy();
  });
});
