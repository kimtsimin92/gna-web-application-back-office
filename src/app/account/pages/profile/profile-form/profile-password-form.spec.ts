import { ProfilePasswordForm } from './profile-password-form';

describe('ProfilePassword', () => {
  it('should create an instance', () => {
    expect(new ProfilePasswordForm()).toBeTruthy();
  });
});
