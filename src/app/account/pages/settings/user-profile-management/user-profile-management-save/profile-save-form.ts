import {FormControl, FormGroup, Validators} from "@angular/forms";

export class ProfileSaveForm {

  // groupCode = new FormControl(null);
  // roleCode = new FormControl(null);
  permissionId = new FormControl(null);
  permissionStatus = new FormControl(null);

}
