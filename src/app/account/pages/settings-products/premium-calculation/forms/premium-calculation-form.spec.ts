import { PremiumCalculationForm } from './premium-calculation-form';

describe('PremiumCalculationForm', () => {
  it('should create an instance', () => {
    expect(new PremiumCalculationForm()).toBeTruthy();
  });
});
