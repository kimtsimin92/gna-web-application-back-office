import { SubscriptionRequestDto } from './subscription-request-dto';

describe('SubscriptionRequestDto', () => {
  it('should create an instance', () => {
    expect(new SubscriptionRequestDto()).toBeTruthy();
  });
});
