import { CoinsuranceRequestDto } from './coinsurance-request-dto';

describe('CoinsuranceRequestDto', () => {
  it('should create an instance', () => {
    expect(new CoinsuranceRequestDto()).toBeTruthy();
  });
});
