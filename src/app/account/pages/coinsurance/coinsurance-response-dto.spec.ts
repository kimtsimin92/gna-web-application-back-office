import { CoinsuranceResponseDto } from './coinsurance-response-dto';

describe('CoinsuranceResponseDto', () => {
  it('should create an instance', () => {
    expect(new CoinsuranceResponseDto()).toBeTruthy();
  });
});
