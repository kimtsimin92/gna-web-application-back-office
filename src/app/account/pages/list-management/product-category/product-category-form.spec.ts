import { ProductCategoryForm } from './product-category-form';

describe('ProductCategoryForm', () => {
  it('should create an instance', () => {
    expect(new ProductCategoryForm()).toBeTruthy();
  });
});
