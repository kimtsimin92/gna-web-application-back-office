import { CustomerAccountResponseDto } from './customer-account-response-dto';

describe('CustomerAccountResponseDto', () => {
  it('should create an instance', () => {
    expect(new CustomerAccountResponseDto()).toBeTruthy();
  });
});
