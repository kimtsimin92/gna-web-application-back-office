import { CustomerAccountRequestDto } from './customer-account-request-dto';

describe('CustomerAccountRequestDto', () => {
  it('should create an instance', () => {
    expect(new CustomerAccountRequestDto()).toBeTruthy();
  });
});
