import { CampaignResponseDto } from './campaign-response-dto';

describe('CampaignResponseDto', () => {
  it('should create an instance', () => {
    expect(new CampaignResponseDto()).toBeTruthy();
  });
});
