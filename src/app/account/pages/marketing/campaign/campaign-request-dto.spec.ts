import { CampaignRequestDto } from './campaign-request-dto';

describe('CampaignRequestDto', () => {
  it('should create an instance', () => {
    expect(new CampaignRequestDto()).toBeTruthy();
  });
});
