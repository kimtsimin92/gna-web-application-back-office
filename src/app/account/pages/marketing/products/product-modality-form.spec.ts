import { ProductModalityForm } from './product-modality-form';

describe('ProductModalityForm', () => {
  it('should create an instance', () => {
    expect(new ProductModalityForm()).toBeTruthy();
  });
});
