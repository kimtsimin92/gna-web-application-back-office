import { ProductPartnerForm } from './product-partner-form';

describe('ProductPartnerForm', () => {
  it('should create an instance', () => {
    expect(new ProductPartnerForm()).toBeTruthy();
  });
});
